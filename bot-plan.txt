Completed the codebase orientation and blueprinting pass. The implementation is now underway, but the new runtime graph is not yet validated for production use. I ignored the root `Modern` folder entirely; `Tools/NavAuthoringLegacy` was inspected only as historical evidence.

## Current implementation status and validation gate

The current alpha implementation is a prototype, not an accepted navigation system.
It can build a compact OG2-derived graph and report abstract graph paths, but an
abstract `spawn -> objective` path is not evidence that a bot can physically
execute the route. The first alpha runtime trial did not complete the outbound
Truefort trip to the enemy intel, so the graph must currently be treated as
untrusted.

This distinction is now an explicit development gate:

```text
Graph reports a path
    -> trace the exact edge sequence
    -> validate each edge against the OG2 movement model
    -> execute the same edge sequence in the runtime
    -> classify the failure as graph or executor
```

Debugging order is mandatory:

1. Establish a deterministic Truefort spawn-to-enemy-intel route. The outbound
   route is the descent over the stair platforms; the carrier return is a
   separate ascent route and must not be used as the first outbound diagnosis.
2. For every generated edge on that route, record its source/target coordinates,
   link kind, completion recipe, support/clearance assumptions, and runtime
   result.
3. If the OG2 movement validator rejects an edge, the graph generator is wrong
   or overly permissive. Fix the graph before tuning steering.
4. If the movement validator accepts an edge but runtime execution fails, fix
   the route executor or steering contract without weakening graph validity.
5. Only after the outbound capture is physically proven should carrier return,
   other objectives, other classes, and the stock-map matrix be expanded.

The current hard acceptance gate is therefore:

- no graph-level reachability result counts as a pass by itself;
- no generated edge may rely on an unverified broad heuristic that the runtime
  cannot execute;
- Truefort outbound capture must pass before return-path work is considered
  complete;
- the full class/map matrix remains blocked until both directions pass on the
  focused Truefort test.

Known current status: Core and the diagnostic tool compile, the alpha graph and
diagnostic harness exist, but Truefort outbound runtime capture is not yet
passing and the requested stock-map/class matrix has not been accepted.

## Immediate pivot: authoritative contact graph

The heuristic alpha graph is not a valid navigation graph and is no longer a
candidate for incremental tuning. In particular, the previous stair/step
profile admitted long links that looked plausible in geometry but could not be
executed by OG2. The graph must not be made to appear reachable by adding broad
relays, objective-specific exceptions, or carrier-only capabilities.

The next implementation milestone is an authoritative local-contact graph:

1. Extract a shared geometry snapshot and topology-change surface intervals.
2. Place only local contact/portal samples at surface boundaries, step changes,
   corridor mouths, objective approaches, and spawn approaches.
3. Run bounded OG2 movement sweeps from those samples in both directions. A
   contact is emitted only when the real `PlayerEntity` movement model reaches
   and settles on the recorded destination surface.
4. Store the resulting directional contacts as geometry capabilities. They are
   never gated by carrying intel, objective role, or carrier state. A non-carrier
   must be able to use the same ascent/descent contact when its class can move
   through it.
5. Collapse equivalent contacts across representative movement profiles into
   one shared topology with conservative physical capability metadata.
6. Convert the validated contact sequence into the runtime graph and execute
   that exact sequence in the bot harness.

The first hard proof is Truefort spawn -> enemy intel. This is the outbound
descent over the stair platforms. The return ascent is a separate proof after
the outbound route passes; it is not carrier-only and must also be usable by a
bot chasing, defending, or otherwise entering the corridor without intel.

Until this contact graph proves that route edge-by-edge, the existing alpha
graph remains diagnostic-only, steering/objective behavior remains frozen, and
no stock-map/class matrix result may be reported as a navigation pass.

### Contact-first implementation checkpoint

The first contact-first implementation is available behind
`BOTBRAIN_NAV_ALPHA_CONTACT_GRAPH=1`. On Truefort with the Scout representative
it currently builds a substantially smaller graph than the heuristic builder
and produces reachable paths in both directions. The runtime reaches enemy
intel, picks it up, and executes the long return stair ascent; it still fails
later in the lower-left transition chain, so this is evidence of progress, not
acceptance. The graph remains opt-in until the exact outbound and return route
passes in the runtime.

The current contact build is also too slow for the final runtime target when
all representative profiles are enabled. The next optimization is spatially
bounded contact seeding and reusable sweep state—not removal of OG2 movement
validation and not a return to broad pairwise heuristics.

## What the current project contains

The relevant runtime is centered in:

- [BotBrainController.cs](<C:/Users/level/Desktop/OpenGarrison Active/OpenGarrison-Fork/Core/BotBrain/BotBrainController.cs:267>)
- [NavGraph.cs](<C:/Users/level/Desktop/OpenGarrison Active/OpenGarrison-Fork/Core/BotBrain/Navigation/NavGraph.cs:35>)
- [BotNavigationAssetBuilder.cs](<C:/Users/level/Desktop/OpenGarrison Active/OpenGarrison-Fork/Core/BotBrain/Navigation/BotNavigationAssetBuilder.cs:812>)
- [BotBrainMovementProbe.cs](<C:/Users/level/Desktop/OpenGarrison Active/OpenGarrison-Fork/Core/BotBrain/Navigation/BotBrainMovementProbe.cs:26>)
- [ObjectiveEvaluator.cs](<C:/Users/level/Desktop/OpenGarrison Active/OpenGarrison-Fork/Core/BotBrain/Objectives/ObjectiveEvaluator.cs:16>)
- [BotBrain.Tools/Program.cs](<C:/Users/level/Desktop/OpenGarrison Active/OpenGarrison-Fork/BotBrain.Tools/Program.cs:166>)

The simulation authority is correctly located in `Core`: `SimpleLevel`, `LevelSolid`, `RoomObjectMarker`, `PlayerEntity`, `LegacyMovementModel`, `SimulationWorld`, control-point systems, intel systems, gates, dropdowns, teleports, jump pads, and moving platforms.

The project also has a substantial diagnostic/test surface:

- navigation cost tests
- no-graph fallback tests
- carrier performance tests
- movement regression tests
- traversal lab tests
- class behavior/combat/medic/spy tests

The current checkout is heavily modified in unrelated areas. I preserved all of it.

## Diagnosis of the existing navigation stack

The current system is not one navigation system. It is several competing systems:

```mermaid
flowchart TD
    O[ObjectiveEvaluator] --> B[BotBrainController]
    B --> G[Cached NavGraph]
    B --> S[SteeringMachine]
    B --> D[Direct Seek]
    B --> L[Local Motion]
    B --> P[Primitive Direct Drive]
    B --> T[Objective Tapes]
    B --> V[Verified Proof Routes]
    B --> C[Map-Specific Repairs]
```

Important findings:

1. The live controller calls `TryLoadCachedGraph`; it does not normally call the available `LoadGraphOrBuild` path. Missing or invalid graphs therefore enter `ThinkWithoutNavigationGraph`, where direct seek and local motion take over.

2. The current asset builder is already a large, sophisticated system, but it is doing too much:
   - surface extraction
   - portal generation
   - pairwise candidate discovery
   - runtime movement simulation
   - class/team certification
   - map-specific repairs
   - authored corridors
   - proof-route integration
   - objective anchoring

3. Existing shipped assets demonstrate the cost problem:

   - roughly 129–1,000 nodes per map
   - roughly 378–9,548 edges
   - up to 42,872 node-pair checks
   - up to 21,128 movement-probe attempts
   - assets ranging from hundreds of KB to over 7 MB

   Some maps still report spawn/objective reachability failures despite this effort, notably Destroy, Dirtbowl, Egypt, and Gallery.

4. `NavGraph` performs brute-force nearest-node searches and allocates fresh arrays and priority queues for reachable-node and path queries.

5. `BotBrainMovementProbe` simulates `PlayerEntity` instances for many candidate edges, across multiple profiles, teams, launch timings, and variants. This is fundamentally too expensive for a runtime-first generator.

6. The graph is not fully class/team agnostic today. Its topology is shared, but edge metadata contains class/team masks produced by probing selected classes—primarily Heavy, Soldier, and Scout—and runtime filtering depends on those masks.

7. Movement ownership is fragmented. `SteeringMachine`, objective tapes, proof routes, direct seek, local-motion recovery, primitive drive, and map-specific methods can all override one another.

8. `ObjectiveEvaluator` is comparatively clean and should remain. Combat, targeting, medic behavior, class behavior, and objective selection should not be rewritten as part of navigation.

The existing tests confirm this architecture. For example, [BotBrainNoGraphFallbackTests.cs](<C:/Users/level/Desktop/OpenGarrison Active/OpenGarrison-Fork/Tests/OpenGarrison.PluginHost.Tests/BotBrainNoGraphFallbackTests.cs:8>) intentionally validates direct-seek behavior when navigation is missing, while [BotBrainNavigationCostTests.cs](<C:/Users/level/Desktop/OpenGarrison Active/OpenGarrison-Fork/Tests/OpenGarrison.PluginHost.Tests/BotBrainNavigationCostTests.cs:9>) encodes several map-specific routing penalties.

## Recommended architecture: an OG2-native surface/portal graph

The replacement should not be a rebuilt version of the old graph. It should be a smaller, runtime-generated graph derived directly from the current OG2 simulation model.

The key design is:

- static geometry topology
- dynamic traversal policies
- objective goal regions
- compact transition links
- deterministic route execution

```mermaid
flowchart TD
    L[SimpleLevel + OG2 collision/object metadata] --> S[Geometry Snapshot]
    S --> X[Walkable Surface Extraction]
    X --> P[Portal and Anchor Extraction]
    P --> E[Analytic Link Generation]
    E --> A[Objective Goal Regions]
    A --> G[Immutable Og2NavGraph]

    G --> Q[Nav Query Context]
    Q --> R[A* Route]
    R --> F[Route Executor]
    F --> I[PlayerInputSnapshot]

    D[Dynamic gates/objective state] --> Q
    C[Class movement profile] --> Q
    T[Team/carrying-intel policy] --> Q
```

### 1. Immutable geometry snapshot

Create a compact `Og2NavigationGeometry` snapshot from `SimpleLevel`.

It should contain:

- world bounds and level fingerprint
- static solid rectangles
- walk-blocking room objects
- dropdown platforms
- team gates and intel gates
- directional walls and barriers
- teleports, catapults, jump pads, and moving-platform metadata
- spawn regions
- intel base regions
- control-point and capture-zone regions
- generator and arena-objective regions

The snapshot must be independent of `PlayerEntity` instances and safe to build once per level.

### 2. Surface regions instead of dense waypoint chains

A surface should be a horizontal walkable interval:

```text
Surface:
    leftX
    rightX
    topY
    clearance
    flags
```

Generate surfaces by taking exposed solid top edges and subtracting intervals blocked by other geometry. Merge adjacent coplanar intervals.

Do not create a waypoint every 80 pixels. A long flat floor should be one surface region with implicit movement along its X interval.

This removes most unnecessary nodes and makes movement naturally smooth.

### 3. Portals and anchors

Create portals only where topology changes:

- surface endpoints
- step transitions
- jump launch/landing bands
- drop/fall edges
- dropdown transitions
- special-object entry/exit points
- narrow corridor entrances
- objective approach points

Objectives should not be represented as one raw center node. They should be represented as goal regions with multiple valid standing slots.

For a control point, the goal region should derive from the actual capture zone, because capture logic uses the zone geometry rather than merely the control-point marker center. A goal region can contain:

- center slot
- left/right slots
- upper/lower slots where geometry permits
- approach slots just outside the capture area

The resolver chooses the closest reachable slot, not the mathematically nearest point.

### 4. Class/team-agnostic links

The graph topology must never be duplicated per class or team.

A link stores physical requirements, not class IDs:

```text
Link:
    fromPortal
    toPortal
    kind
    baseCost
    horizontalDistance
    verticalDelta
    requiredClearance
    requiredJumpHeight
    requiredFallCapacity
    executionRecipe
    dynamicPolicyReference
```

At query time, a `NavTraversalContext` supplies:

- player class movement profile
- team
- carrying-intel state
- current dynamic gate state
- objective mode/state

This preserves one graph while allowing the runtime to reject links a specific agent cannot use.

Team gates, barriers, and intel gates should be policy checks, not separate graphs.

### 5. Contact-first link generation

Analytic geometry is a candidate prefilter only. It may bound the local sweep
neighborhood and reject impossible clearance/range pairs, but it cannot certify
that a link is traversable. Every emitted step, jump, fall, and dropdown link
must come from a bounded OG2 movement sweep that records the actual contact and
settled destination.

The build pipeline is:

1. snapshot level geometry
2. extract exposed walkable intervals
3. merge/prune surfaces
4. create topology-change contacts and objective/spawn approach samples
5. query only spatially adjacent contact candidates
6. run short directional OG2 sweeps from each candidate in both directions
7. emit only contacts with a settled destination and an executable recipe
8. validate connected components and objective reachability
9. publish one immutable graph

This deliberately spends bounded simulation time on local contacts instead of
spending unbounded time on pairwise heuristic edges. Runtime probing is not a
fallback after generation; it is the authority for ambiguous traversal.

## Runtime query and movement model

The new runtime should provide:

```text
Og2NavWorld
    Immutable graph
    Spatial attachment index
    Dynamic policy/version state
    Reusable pathfinding scratch buffers

NavQueryContext
    Class profile
    Team
    Carrying intel
    Current objective state

NavRoute
    Surface corridors
    Transition links
    Goal region/slot

NavRouteExecutor
    Walk corridor
    Jump recipe
    Drop recipe
    Special transition recipe
```

Key runtime behavior:

- attach the bot to the nearest valid surface interval using a spatial index
- attach the objective to a reachable goal slot
- run A* over portals/surface regions
- move along a surface corridor implicitly
- execute transitions using explicit link recipes
- replan only on route invalidation, failed transition, major goal change, or periodic health interval
- use reusable arrays/queues so steady-state navigation allocates nothing

`FindNearestReachableNode` should disappear. The route query should directly search for the best reachable goal slot using the current context.

## Dynamic state handling

The static graph should not be rebuilt when:

- a control point changes ownership
- a player carries intel
- a setup gate expires
- a team gate changes accessibility
- a forward-spawn state changes

Instead, these become dynamic policy checks attached to links.

The existing semantics in `SimpleLevel.GetBlockingTeamGates`, `PlayerEntity.CanOccupy`, control-point capture logic, and intel logic should be centralized behind a navigation-access policy so the graph and simulation agree.

Dynamic state should have a small version counter. Routes cache the version they were planned against and invalidate when relevant policy changes.

## Objective integration without changing behavior

Keep `ObjectiveEvaluator`, target selection, combat decisions, medic logic, spy behavior, and class behavior unchanged.

Only replace the travel seam:

```text
Existing behavior logic
    -> existing objective/target decision
    -> navigation goal resolver
    -> new graph route
    -> new route executor
```

The first integration can continue accepting the evaluator’s `(X, Y)` output, with a resolver that recognizes nearby control points, intel bases, generators, and capture zones. Later, the evaluator can emit a richer goal descriptor without changing its decision policy.

This removes the need for:

- objective tapes
- verified proof graphs
- authored corridors
- CTF-specific return graph hacks
- map-specific direct-seek repairs
- per-objective direct-drive branches

Direct movement should remain only as a short-lived emergency recovery mechanism when a route transition fails—not as a second navigation system.

## Performance targets

These should become acceptance criteria after benchmarking:

- one graph build per `SimpleLevel`, shared by all bots
- no full `PlayerEntity.Advance` loop for normal graph construction
- no O(N²) portal pairing
- build target: under 100 ms for typical stock maps and under 250 ms for the largest stock maps
- steady-state path query: under 0.25 ms per bot, with zero allocations
- nearest surface/goal attachment through the spatial index, not full graph scans
- route recomputation only on invalidation or meaningful goal changes
- materially fewer nodes/links than current assets, with no fixed waypoint spacing

The graph should be built synchronously during level setup or match preflight. Background building can be added later, but it should not be required for correctness.

## Migration plan

### Phase 0: establish the contract

Add instrumentation for:

- graph build duration
- path query duration
- allocations
- route failures
- transition failures
- stuck duration
- objective reachability
- capture/pickup/return/score completion

Preserve existing behavior tests as regression tests.

### Phase 1: build the new graph offline/runtime-shared

Implement the geometry snapshot, surface extraction, portal graph, objective slots, and diagnostics in `BotBrain.Tools`.

Reuse the existing map-loading and traversal-lab infrastructure, but do not reuse the old graph builder as production code.

### Phase 2: integrate behind a feature switch

Add the new navigation service beside `NavGraph`.

Run both systems against the same maps and compare:

- graph size
- build time
- route length
- route completion
- stuck/repath counts
- objective reachability

### Phase 3: replace movement ownership

Make the new route executor the sole owner of navigation movement.

Keep combat steering and objective selection intact. Reduce direct seek/local motion to bounded transition recovery.

### Phase 4: retire legacy navigation layers

After map-matrix validation, remove live dependencies on:

- `BotNavigationAssetBuilder`
- `BotBrainMovementProbe` as a general edge generator
- objective tapes
- proof graphs
- authored corridors
- map-specific navigation repairs
- graphless objective travel

The legacy assets may remain temporarily for comparison, but should no longer be required to start a match.

## Validation matrix

The replacement should be accepted only after deterministic validation across every shipped map and area:

- all spawn regions attach to the graph
- every objective has at least one reachable goal slot from each team’s spawn component
- control-point zones are enterable and capturable
- intel can be picked up, returned, and scored
- generators and arena objectives are reachable
- dropdowns, stairs, jumps, falls, gates, and narrow corridors work
- all classes can route where their physical profile permits
- both teams receive the same topology
- carrying intel changes policies without rebuilding topology
- no route requires an authored tape or map-specific repair

The targeted navigation/traversal tests did not complete within the two-minute diagnostic window in this dirty checkout, so I am not treating that run as a pass or failure.

The architectural decision is clear: build one compact, immutable, OG2-native surface/portal graph per level; keep class/team differences in runtime link eligibility and policies; keep objectives and combat behavior unchanged; and make one route executor the only normal source of bot traversal.

## 2026-07-27 validation correction and active checkpoint

The earlier checkpoint above is stale in one important respect: the contact-first
graph has now proven the focused Truefort outbound descent and carrier return for
the exact Pyro movement signature on both teams (2/2). Exact Spy and Quote
contacts also pass both teams (4/4), and exact Soldier and Heavy contacts pass
both teams (4/4). This is runtime capture evidence, not merely abstract graph
reachability.

The graph is still not accepted or default. A Heavy-only capability envelope
passed 10/16 ordinary-class Truefort trials; the failures were caused by
movement-capability mismatches, not evidence that the route itself is globally
invalid. The exact-signature runs corrected those failures for Spy, Quote,
Soldier, and Heavy. The remaining acceptance work is to validate the other
movement signatures, resolve the intended test contract for support/defensive
classes without changing their production behavior, reduce the current contact
sweep build cost, and run the requested stock-map matrix.

The current graph contains two deliberate state distinctions that must remain:

- every contact is certified from the real OG2 movement model for a movement
  signature; class IDs are only a runtime capability mask;
- carrying-intel and non-carrying contacts are separate policy variants, and
  same-surface walk spans are checked for carrier clearance so a team gate that
  blocks the carrier cannot be crossed by a generic walk edge.

The current alpha graph is still opt-in through
`BOTBRAIN_NAV_ALPHA_CONTACT_GRAPH=1`. Do not make it default until the full
map/class capture gate passes. The current build cost is roughly 130-145 seconds
for two representative movement signatures on Truefort, so generator
optimization is now a first-class task; removing OG2 validation is not an
acceptable optimization.

## 2026-07-27 composability checkpoint

The immediate bottleneck is now identified: individual contact edges can be
OG2-valid in isolation while the A* route assembled from them is not
state-composable. A live carrier can enter the next jump with residual
horizontal momentum from the preceding edge; the current edge contract does
not represent that incoming motion state. On Truefort the shared graph chose
the `158 -> 361 -> 436 -> 121` carrier route and repeatedly re-entered the
`361 -> 436` jump, while the Heavy-only graph chose a different validated
contact sequence. This is a graph/runtime contract failure, not justification
for weakening launch validation or restricting ascent edges to carriers.

The next implementation step is state-composable contacts: retain OG2
movement proof, record the source/launch state needed by each transition, and
make route selection reject or penalize incompatible predecessor-to-edge
transitions. A compact velocity/grounded state bucket or explicit setup
contact is preferable to ad-hoc speed slack. Full capture by every class on
all requested stock maps remains the acceptance gate before defaulting alpha.

## 2026-07-27 corridor evidence and rejected experiment

The latest Truefort evidence narrows the composability failure further. Soldier
and Engineer now pass both teams (4/4) after the measured positive-trigger
launch correction. Quote still fails at the stair-pocket transition around
x=2854: the bot is grounded against a low-clearance step edge, while the
shared graph has the neighboring platform nodes but no stable node representing
that launch pocket. Reattaching to a distant platform node is therefore
incorrect, and the current nearest-node API needed an alpha-only lower
attachment bound plus stale-first-waypoint invalidation.

An attempted fix that materialized raw solid-edge pockets was rejected. It grew
Truefort from 93 surfaces to 782 and degraded routing, proving that raw solid
edges are not a suitable node source. The correct next step is a bounded
OG2-probed contact/setup state for short-lived ledge launch pockets, with
class-specific capability validation and explicit predecessor compatibility;
the graph must not be expanded by indiscriminate geometry tessellation.

The alpha graph remains opt-in and unaccepted. The latest build compiles with
zero errors; no default switch or full stock-map acceptance claim is justified
until the pocket/setup state and remaining class routes pass the matrix.

## 2026-07-28 graph gate and roster checkpoint

The validation work has now been split into two explicit gates:

- The graph-only gate validates finite nodes, valid edge metadata, objective
  attachment, class/team filters, carrying-intel variants, and filtered paths
  from every spawn to the required CTF objectives. Truefort passes this gate
  for the full roster: 200/200 filtered routes, 568 nodes, zero structural
  issues. This proves abstract topology support only; it is not runtime
  capture acceptance.
- The runtime capture gate executes actual PlayerEntity movement. Quote now
  completes a full Truefort capture after contact edges honor their measured
  airborne travel direction. The trace reaches enemy intel, traverses the
  reverse stair chain, clears 205 -> 202, and captures.

The first full Truefort roster runtime matrix is not green: 9/20 trials passed
(both teams, ten classes). Red Scout/Pyro/Heavy and Blue Scout/Soldier/Heavy/
Demoman/Spy/Sniper/Quote still fail. The failures are spread across several
OG2 contacts and setup/landing transitions, so the graph is not yet validated
for the whole roster and alpha must remain opt-in/default-off.

The current concrete contract is that an isolated OG2 contact recipe is not
enough: the executor must preserve the probe's airborne direction and the
graph must eventually encode compatible incoming grounded/velocity state for
successive contacts. A blanket trigger-tick fallback was tested and reverted
because it regressed the first reverse stair edge. A contact-specific airborne
direction fix is retained because it matches the OG2 probe and made Quote's
205 -> 202 transition pass. The rejected launch-node materialization and tight
handoff-radius experiments remain reverted.

Next acceptance work is graph/runtime composition validation and the remaining
roster failures, followed by the eight-map matrix requested in the task. No
default switch is justified until those runtime gates pass.

## 2026-07-28 runtime-state follow-up

The broad contact-source gate was tested and rejected. Requiring a live bot to
be within a small fixed position tolerance of every contact source regressed
Heavy outbound traversal before intel, so it is not a valid general solution.

The OG2 contact generator itself was rechecked on the Heavy carrier transition
represented by graph nodes `239 -> 207` (surface ids `80 -> 70`). The carrying
probe records an actual carrying state, launch near `(4428,822)`, and landing
near `(4606,756)` on the target surface. The raw movement diagnostic was also
corrected to match the generator's setup and documented internally: generator
sweep ticks are zero-based, while the diagnostic's `--jump-tick` is one-based.
The equivalent raw probe reaches the same target surface.

The remaining failure is runtime route-state composition. A route can enter a
grounded-only contact while still airborne from a preceding fall, or enter a
valid contact from the predecessor completion corridor with a different
run-up distance. The executor now uses the edge's measured launch window to
derive an entry-position run-up delay and refuses to hand off to a grounded
OG2 contact while airborne. Those changes are generic graph-metadata rules,
not carrier-only or class-specific restrictions, but the full Truefort Heavy
capture is still not green, so they are not acceptance evidence yet.

Current status remains: graph-only topology is structurally valid for the
full Truefort roster, but runtime graph validity is not proven. The next
implementation target is an explicit, bounded contact entry-state contract
(grounded/launch-direction/run-up state) that is represented in route
selection or contact setup, followed by focused Truefort composition tests
before any wider stock-map matrix or alpha default switch.

## 2026-07-28 regression-report correction

The earlier observation that Heavy reached intel and then failed on the return
route is historical evidence from a pre-subsequent-change run. It was not
revalidated after later graph recipe, steering, and runtime-contact changes.
The latest focused Red Heavy runs instead fail outbound before pickup. Therefore
the earlier result must not be treated as maintained behavior, and the prior
`15/20` Truefort runtime count must not be reported as the current acceptance
state.

Going forward, every navigation change needs a fixed smoke/regression run before
its result is used: at minimum the affected class/team/map route plus the full
Truefort matrix before claiming a maintained pass count. Historical passes and
structural graph-route passes remain useful diagnostics, but are not runtime
acceptance evidence after an unregressed change.

## 2026-07-28 post-change Truefort matrix

The current source was rebuilt and the complete Truefort alpha runtime matrix
was rerun: 10 classes x 2 teams, 20 capture trials, 9000 ticks per trial.
The result was `16/20` passed and `4/20` failed in 407.9 seconds.

Passed: Red Scout, Pyro, Demoman, Medic, Engineer, Spy, Sniper, Quote;
Blue Scout, Pyro, Soldier, Heavy, Medic, Engineer, Spy, Quote.

Failed: Red Soldier (carrying, stalled on the return route), Red Heavy
(stalled outbound before pickup), Blue Demoman (carrying, fell/stalled on the
return route), and Blue Sniper (carrying, lost its route). The aggregate is one
better than the historical `15/20`, and Blue Heavy now passes, but the failure
set changed. Because the earlier per-trial baseline was not preserved, this
result does not prove that no individual class/team regressed.

## 2026-07-28 diagnostic iteration speed-up

The alpha graph is now persisted as a compact binary snapshot under the
OpenGarrison user config directory. Its key includes the OG2 generator
fingerprint, map geometry fingerprint, map area/mode, contact-class set, and
sweep settings. Steering/runtime changes reuse the exact graph; generator or
map changes invalidate it. The current generator fingerprint is
`og2-contact-20260728-v1` in `Og2NavigationGraphBuilder` and must be bumped for
graph-generation logic changes.

The Truefort full-roster graph was verified to load from cache in approximately
one second and still pass the structural `200/200` graph gate. The subsequent
full runtime matrix reused that graph and completed in approximately 128
seconds instead of paying the approximately five-minute generation cost.

`--alpha-capture-matrix` now accepts `--graph-classes` separately from
`--classes`, allowing a small set of failing trials to run against the exact
full-roster graph. Use short `--capture-ticks` values for targeted smoke runs;
retain the full tick budget for acceptance checkpoints. This prevents a single
runtime/steering change from requiring a cold graph build and 20 long trials.

## 2026-07-28 regression recovery and Truefort acceptance

The subsequent 9/20 regression was traced to a graph-construction defect, not
aiming or player facing. `AddAnchor` connected each spawn anchor to several
nearby surfaces without constraining elevation, producing an invalid ordinary
walk edge from the Blue spawn `(3702,456)` directly to the lower platform
`(3690,570)` (`555 -> 141`). The graph was structurally connected but that edge
was not a valid traversal contract. Spawn anchors now directly attach only to
surfaces within a 24-pixel vertical band; cross-level movement must come from
same-surface corridors or OG2-certified contacts. The generator fingerprint is
`og2-contact-20260728-v3`, invalidating the prior snapshot.

Runtime contact tracing also found that the executor was counter-steering every
other tick against the measured launch-speed window. That diverged from the
runtime probe, which holds its measured launch direction until the recorded
jump tick. Runtime-resolved contacts now follow that exact measured run-up.

After the graph rebuild (568 nodes, 14,655 edges; cold build approximately 255
seconds), the cached full-roster Truefort runtime matrix passed `20/20` in
72.5 seconds at 3,800 ticks per trial. Every class passed for both Red and
Blue. This is the first current, unregressed Truefort acceptance result; alpha
remains opt-in until the remaining stock-map gates pass.

## 2026-07-28 Eiger acceptance

The same anchor-elevation defect appeared on Eiger at objective anchors: lower
floor nodes `(622,978)` and `(4232,978)` had invalid ordinary-walk edges to
objectives `(627,822)` and `(4231,822)`. The shared anchor vertical tolerance
was applied to objective anchors as well, producing generator fingerprint
`og2-contact-20260728-v4`. The corrected Eiger graph contains 356 nodes and
11,063 edges.

The affected Red Demoman, Blue Heavy, and Blue Demoman trials all pass after
the fix. The complete Eiger matrix then passed `20/20` (all 10 classes on both
teams) in 11.6 seconds using the warm graph cache. Truefort and Eiger are now
both current full-roster runtime gates; alpha remains opt-in while the other
requested stock maps are tested.

## 2026-07-28 team-aware graph and Truefort regression recovery

The generator now probes both Red and Blue collision contexts while building
the shared graph. This is required for OG2 maps with asymmetric team gates:
the same geometry remains class-agnostic while edge capability records retain
the team masks under which each contact was actually certified. The generator
fingerprint is now `og2-contact-20260728-v5`; generator changes invalidate the
persistent binary cache, while steering-only changes reuse it.

The first v5 Corinth run exposed a real execution problem: generic semantic
walk-climb and pressed-obstacle jump recovery were overriding certified OG2
contacts. Those fallbacks are now suppressed while an OG2 contact edge is
active. Runtime contact resolution also waits for a grounded retry after a
preceding airborne edge instead of continuing static steering with an invalid
live state.

The current full-roster runtime gates are:

- Corinth: `20/20`, 300 nodes, 8,252 edges, warm-cache runtime.
- Eiger: `20/20`, 359 nodes, 11,349 edges, warm-cache runtime.
- Truefort: `20/20`, 587 nodes, 15,608 edges, warm-cache runtime.

Truefort required one final generic fix. Red Pyro entered the certified
`258 -> 228` launch corridor above its 100--117 horizontal-speed window and
the old executor began braking one update too late. The executor now starts
the measured counter-steer when the next fixed update would enter the launch
band above its certified speed. Red Pyro then completed at tick 2,847, and
the complete Truefort matrix passed again with no graph rebuild.

This is the first checkpoint where the graph and runtime executor are both
validated for the full roster on three stock maps. The remaining five
requested maps still need the same full matrix; alpha must not become the
default until those gates are green.

## 2026-07-28 remaining-map fixture audit

The requested Docking matrix was attempted before proceeding with the other
maps. The diagnostics normalized `Docking` to `cp_docking_v2`, but that map
asset is not present/importable in this checkout, so the run stopped at map
load with zero trials. This is a fixture-availability block, not a navigation
pass or fail, and must not be counted in the acceptance denominator. Harvest,
Conflict, Gallery, and Valley remain to be exercised wherever their stock
assets are available; any additional missing fixture will be recorded the
same way rather than silently substituted.

## 2026-07-28 current validation checkpoint

The generator/runtime transition has now passed the complete available stock-map
matrix after the latest runtime handoff fix. The current OG2 generator fingerprint
is `og2-contact-20260728-v18`; persistent graph snapshots are keyed by that
fingerprint and the map geometry, so steering-only changes reuse the graph while
generator changes rebuild it.

The final warm-cache acceptance runs completed as follows:

| Map | Mode | Graph | Runtime trials |
|---|---|---:|---:|
| Truefort | Capture the Flag | 603 nodes / 7,487 edges | 20/20 |
| Conflict | Capture the Flag | 359 nodes / 4,214 edges | 20/20 |
| Eiger | Capture the Flag | 369 nodes / 5,046 edges | 20/20 |
| Docking | Control Point | 268 nodes / 3,273 edges | 20/20 |
| Corinth | King of the Hill | 305 nodes / 3,872 edges | 20/20 |
| Harvest | King of the Hill | 197 nodes / 2,196 edges | 20/20 |
| Gallery | King of the Hill | 333 nodes / 4,132 edges | 20/20 |
| Valley | King of the Hill | 418 nodes / 4,787 edges | 20/20 |

That is 160/160 completed class/team objective-capture trials across all eight
requested stock maps. Truefort and Conflict include full outbound intel
pickup and carrier return scoring for all ten classes on both teams. The other
maps use their imported Control Point or KOTH objective rules and therefore
validate objective arrival/capture rather than a CTF return leg.

Docking's package is stored under the repository-level `Maps/cp_docking_v2`
directory rather than the default built-output map directory. The acceptance run
sets `OPENGARRISON_MAPS_DIR` to that existing fixture directory; the diagnostic
then imports it as `cp_docking_v2` and completes all 20 trials. No map source was
substituted or modified.

The latest execution defect was not an aim-direction issue and was not fixed with
map- or class-specific routing. A grounded-start contact could be handed off one
simulation tick while the live body was still airborne. The controller then
suppressed every input while waiting for grounded runtime proof, freezing Red
Soldier and Red Sniper on Truefort's return route. The controller now lets the
steering layer apply horizontal recovery toward the unresolved contact while
explicitly suppressing the jump pulse until grounded; runtime OG2 proof still
begins from the real live state. The formerly failing Truefort subset then passed
6/6, followed by the full 20/20 matrix.

The alpha navigation path is now the default for normal bot controllers:
`BOTBRAIN_NAVIGATION_MODE=legacy` or `asset` is required to opt back into the
legacy asset path. The contact graph remains runtime-generated and persistent;
the graph is shared across bots and does not rebuild when only steering changes.
The default-path practice-game smoke test should still be run interactively
against these cached graphs; the automated acceptance matrix itself is green.

## 2026-07-28 expansion audit correction

The preceding eight-map matrix is not a valid current acceptance result for
the graph-generator expansion. It was recorded before the controlled cold
graph gate was rerun and must not be used as evidence that the new generator
supports the remaining stock maps.

The post-crash controlled gate is currently negative:

- `TwodFortTwo`: `0/160` class/team graph routes; cached and cold rebuilds both
  produce a disconnected upper stair route (`305` nodes, `2,779` edges).
- `Waterway`: `0/360` graph routes (`466` nodes, `4,848` edges).
- `Dirtbowl`: `480/2,000` graph routes and `0/20` runtime captures.
- `Egypt`: `1,600/4,500` graph routes and `20/20` runtime captures; the runtime
  result does not override the failed graph gate.

Expansion is halted until the graph topology is validated. No nearest-
reachable fallback, class-specific steering tweak, or capture-only result is
an acceptance pass. The failure is generator topology: OG2 stair routes use
narrow six-pixel step supports plus compound movement programs, while the
current contact graph only retains fully standable surfaces and certifies one
fixed-direction jump per contact.

The package inventory was also corrected before expansion resumes. The 16
packages `dj_Delta_v1`, `dj_Loop_of_Fate`, `dj_Magik`, `dj_thundermountain`,
`jt_aperture`, `jt_glados`, `nfa_readyup`, `nfa_transition`, `rj_2dfortremix`,
`rj_classicwell`, `rj_conflict`, `rj_duality`, `rj_facility_colored`,
`rj_lavafort`, `rr_sandzone2`, and `vip_docking` are excluded from graph
coverage. The remaining 48 packages collapse to 42 graph builds because six
pairs share an identical walkmask: `ctf_eiger`/`ctf_eiger_plx`,
`ctf_hemisphere`/`ctf_hemisphere_plx`, `koth_corinth`/`koth_corinth_plx`,
`koth_valley`/`koth_valley_plx`, `koth_bayou`/`koth_styx`, and
`dkoth_kots`/`dkoth_kots_a3`.

The next generator milestone is an OG2-authoritative compound contact:
bounded simulation may use narrow step lips internally, may issue repeated
jump pulses and direction changes, and may emit an edge only after landing on
the target stable surface. Its measured control schedule must be replayable at
runtime. Only after that topology gate is green will the 42 unique walkmask
representatives be expanded and aliases mapped to the same graph.

## 2026-07-28 runtime-replay audit

The TwodFortTwo graph was audited at the edge-recipe level before any further
map expansion. The previous v54/v55/v56 Pyro graph gate reported `16/16`
abstract routes, but the cache-hit live capture matrix still reported `0/2`
Pyro captures. The failing Red route repeatedly stopped at `209->229`, a
lower-floor-to-upper-platform edge whose stored recipe was only
`jump + 25 neutral ticks`; it could not reach the target surface in the live
simulation. Blue failed at an analogous upper-stair contact.

The audit found two generator defects: contact-edge merging discarded entry
and exit sample buckets and could retain the first recipe instead of the
compound recipe; and the retained compound contact could have a launch point
outside its source surface after surface-node clamping. v55 bucketed contact
edges and v57 added the source-launch invariant. v57 correctly rejected the
invalid topology (`0/16` Pyro graph routes), proving that the old `16/16` gate
was only an abstract connectivity result. v58 additionally seeds all
standable-width surfaces, including the 24-pixel intermediate stair landing,
but still reports `0/16`; the explorer is not yet emitting the required
intermediate landing chain.

Runtime resolver work is therefore paused behind generator validity. The
resolver must preserve the graph-certified control direction (it must not aim
at the completion-window center), must validate only the first jump's grounded
state for multi-jump schedules, and must eventually use bounded state-keyed
retry rather than permanently abandoning a grounded edge after one failed
probe. None of these runtime changes constitutes a graph acceptance pass.

## 2026-07-29 current-authority route certificate and generator-cost audit

The current PlayerEntity physics now has a separate whole-route certificate
replay. It records every confirmed grounded surface transition and preserves
the exact per-contact entry/exit prediction state, jump-held state, and action
window. The explorer also carries PredictionState and PreviousJumpHeld across
compound-program segments instead of reconstructing each segment from only
position and velocity. This is required groundwork for composing contacts;
it is not, by itself, a graph or live-controller acceptance gate.

The ten checked-in TwodFortTwo pickup proof tapes (five representative stock
movement profiles on both teams) all replay continuously through current
PlayerEntity authority to enemy intel:

| Team | Engineer | Heavy | Pyro | Scout | Soldier |
|---|---:|---:|---:|---:|---:|
| Red | pass | pass | pass | pass | pass |
| Blue | pass | pass | pass | pass | pass |

These are route-proof passes only. They do not prove that the runtime-generated
contact graph contains the same contacts, that the runtime resolver can execute
the stored recipes, or that the remaining roster profiles are covered.

Generator fingerprint `og2-contact-20260729-v74-bounded-compound-program-bank`
now uses frontier-scoped control-program seeds and a smaller bounded compound
program bank. A cold Pyro/Red/non-carrier TwodFortTwo build fell from about
262 seconds to about 215 seconds and produced a graph whose filtered gate was
`4/16`: the four Red outbound variants passed, including the newly certified
final narrow-stair transition from surface `21` to intel surface `4`.
The deliberate context filter excluded Blue and carrier contexts, so the
remaining twelve failures are expected and are not acceptance evidence.

A combined all-context cold build exceeded ten minutes without completing and
was stopped. This is now an explicit generator performance blocker: the graph
must be generated once and then reused from the persistent cache, but the cold
builder is still too broad to be the normal iteration loop. The next generator
step is to separate reusable base-contact evidence from bounded context
compound certification, then run the full two-team/carrying matrix against one
complete artifact. Until that artifact and the live runtime capture matrix are
green, alpha remains an experimental default path for diagnostics/practice only
and no full-map or full-roster claim is valid.

### Generator-cost follow-up (v75-v81)

The phase-timed cold-build baseline is now explicit. On the deliberately
narrow TwodFortTwo Pyro/Red/non-carrier context (32-tick ordinary sweep,
16 ordinary build workers, 16 compound expansions, frontier limit 8), v75
measured approximately 13.3 seconds in ordinary contacts and 202.1 seconds in
compound certification, for 215.6 seconds total. This confirms compound
certification—not geometry extraction or ordinary OG2 contact sweeps—is the
generator bottleneck.

The following measured cuts are retained in the current generator:

- v76 reuses one landing probe per compound schedule and skips repeated
  same-surface landing scans only while the PlayerEntity remains within the
  certified surface interval and within 1.5 pixels of its top. The build was
  208.3 seconds with unchanged filtered reachability (4/16).
- v77 seeds control programs from existing compound/base-contact exit states
  and two fallback samples per selected frontier surface. This changed the
  graph shape but did not improve wall time by itself.
- v78 bounds the control-program expansion queue to 32 for the shallow pass and
  v79 removes the redundant direct-run/short-stair compound programs already
  covered by ordinary contacts. The measured cold build reached 165.1 seconds
  with the same filtered result (4/16).
- v80 bounds the deep retry to 64 expansions by default. The same build reached
  105.7 seconds, still with 4/16 in the intentionally filtered context.
  Experiments at retry limits 16 and 32 were faster but lost the outbound
  compound route (0/16), so 64 is currently the minimum observed route-safe
  retry for this case.
- v81 skips compound certification for a specific class/team/carry context
  only when the already-certified base contact closure reaches that context's
  objective surface. This preserves compound work for contexts that need it.

The full representative-roster TwodFortTwo build was rerun after v81 with
16-way compound context parallelism and exceeded 10 minutes without producing
a graph artifact; it was stopped cleanly. Therefore the full graph is still
not validated, no all-roster capture claim is authorized, and the runtime
alpha default must remain gated. The next implementation task is reusable
per-context evidence/graph assembly (or an equivalent context-sharing design),
not further blind increases/decreases of heuristic expansion limits.

## 2026-07-29 process postmortem

The eight-hour TwodFortTwo focus was a process failure, not evidence that the
map's geometry intrinsically requires ten minutes. The measured facts are:

- A narrow Pyro/Red/non-carrier TwodFortTwo build took about 106 seconds under
  v80. Its base contact phase took about 13 seconds; compound certification
  took about 92 seconds.
- The run that exceeded ten minutes used the full representative movement
  roster, both teams, and both carrying states. The generator replays expensive
  OG2 PlayerEntity control programs independently for those contexts. Map
  surface count is therefore not the dominant cost; context multiplication and
  repeated compound exploration are.
- Each context could perform a shallow exploration and then restart from the
  initial seeds for a deep retry. A graph artifact was written only after all
  contexts completed, so an interrupted build produced no reusable partial
  evidence.
- Generator fingerprint changes from v74 through v82 invalidated prior alpha
  graph-cache keys. We did not maintain a committed, known-good graph artifact
  set or a per-context resumable evidence cache before iterating, so changes
  could not be compared against the previously passing map set cheaply.
- The ten current-authority TwodFortTwo proof-tape replays are valid standalone
  route evidence, but they are not graph-gate or live-capture evidence. The
  filtered graph result remained 4/16, and the full roster graph was never
  validated.
- The first parallel expansion run reported mostly `load_failed` because the
  custom package search path was not set to the repository `Maps` directory;
  only built-in Eiger loaded. That run is invalid map coverage. The corrected
  runs were stopped before completion and are also not coverage evidence.

The operational mistakes were continuing to tune the hardest map after the
generator gate was known negative, treating route certificates as a proxy for
graph acceptance, failing to pin a known-good baseline before changing the
generator fingerprint, and launching the shipped-map matrix before the harness
was configured for custom packages. Consequently, we do not know that the
other maps remained passing after the v74-v82 changes; that assumption would be
unsafe.

The next work item is not another TwodFortTwo heuristic tweak. Freeze the
current experiment, implement context-scoped resumable compound evidence with
exact OG2 prediction state, correct the package-aware canonical map harness,
then run a small canary set against a preserved baseline before expanding to
all 42 graph representatives. No alpha-default or full-roster claim is
authorized until that canary and the complete capture matrix are green.

## 2026-07-29 recovery and regression finding

The v82 experiment was regenerated on Truefort and failed the graph gate:
`518 nodes`, `19,397 edges`, `0/200` filtered roster routes. An unfiltered
route existed, but its route depended on transitions marked
`RequiresCarryingIntel`; this is not acceptable for ordinary traversal and
confirms a graph capability regression.

The prior known-good v18 artifact was not deleted. The preserved cache was
verified through a clean HEAD-based build: `603 nodes`, `7,487 edges`,
`200/200` filtered Truefort routes, zero issues, in about 1.5 seconds from
cache. The active source was then restored to the v18 generator/navigation
baseline and rebuilt successfully. The v82 experiment is preserved separately
at Git ref `refs/codex/recovery-experimental`
(commit `286d3d5f848e8c61d75008c035344c63b687fc4a`).

This establishes the recovery rule: no generator change may replace the
known-good path or invalidate its cache format without first passing the
Truefort `200/200` canary and retaining a directly runnable rollback.

## 2026-07-29 v28 measured-launch checkpoint and dynamic-goal integration

The active contact graph is now fingerprinted as
`og2-contact-20260729-v28-measured-stair-launch-height`. v28 corrected a
runtime-composition defect: a stair contact's jump recipe must use the OG2
probe's measured launch height, not the source surface height. On Truefort,
the isolated Red Heavy carrier-return failure passed after this correction.

The current v28 runtime evidence is:

- Truefort: `20/20` class/team captures, all ten classes on both teams.
- The seven original imported representatives Eiger, Corinth, Docking,
  Harvest, Conflict, Gallery, and Valley: `20/20` each.
- `cp_coldfront_v7` has a v28 graph artifact (`420` nodes, `23,399` edges),
  but its v28 live matrix still must be rerun. Its earlier `20/20` result was
  against v27 and is not silently carried forward after the fingerprint bump.
- The next seven newly built artifacts are `3cp_kistra`, `cp_gully`,
  `cp_thundermountain_d`, `ctf_conflict2_v3`, `ctf_conflict_plx`,
  `ctf_conflict_pro`, and `ctf_conflict_srs_red`. They have graph-build
  evidence but no accepted live capture result yet.

This means the current accepted v28 capture count is eight representatives,
`160/160` trials. Graph-build completion and structural route reports are not
substitutes for live capture acceptance; the stopped long-tail validator is
not counted as a pass.

The graph is not an objective-only route. Spawn, intel, capture-zone, and
generator coordinates seed surface discovery, but the builder expands the
whole reachable surface component by probing both directions with OG2
movement. It emits local walk, jump, fall, and dropdown contacts, records
class-signature/team/carrying-intel capabilities, and then attaches objective
anchors to the resulting network. Runtime goals are coordinates: the
controller attaches the current position and goal to reachable graph nodes and
searches the graph, so moving targets do not need baked graph nodes.

Dynamic-target coverage is now explicitly split from static graph coverage.
Alpha no longer suppresses `TryResolveCaptureTheFlagDynamicObjectiveSeek`.
Enemy carriers can call `TryRouteToDirectSeekTarget` using the alpha graph,
including same-level targets; dropped-intel routing already used this graph
route. A focused alpha regression and the existing legacy engineer-carrier
regression both pass. Dedicated dynamic CTF pursuit still needs live-map
scenario coverage, and arbitrary knockback recovery remains an explicit test
requirement rather than an assumption from ordinary capture passes.

The fast-test rule remains: graph caches are keyed by generator fingerprint,
map fingerprint, class set, and sweep settings; steering/controller changes
must reuse the graph cache. Independent class/team trials may run concurrently,
but a missing cache is prewarmed once before fanout to avoid duplicate cold
builds. Any interrupted or malformed fanout is discarded as harness evidence,
not reported as a map result.

## 2026-07-29 v29 capture-zone anchor validation and dynamic CTF coverage

The capture-zone overlap correction is now fingerprinted as
`og2-contact-20260729-v29-capture-zone-overlap-anchors`. A capture-zone center
is not necessarily contained by one extracted surface interval: on 3cp Kistra,
CP2's gameplay zone spans the adjacent surface boundary. The builder now
attaches capture-zone anchors to the nearest supporting surface overlap inside
the zone bounds, while preserving the zone center as the objective completion
coordinate. This produced an explicit objective node at `(2355,885)` on
3cp Kistra instead of dropping the anchor and routing bots to `(2328,882)`.

The v29 Kistra graph rebuilt in 107.6 seconds with `482` nodes and `29,734`
edges. Its accelerated 20-case capture screen reached `19/20`; all classes
passed for Blue and all Red classes except Spy. The remaining Red Spy failure
is a controller capture-staging `captureStrafeHop` hold at the edge of the
zone, not a missing graph anchor, and is intentionally tracked separately from
this generator change.

Alpha dynamic CTF objective resolution is now graph-backed for all three
dynamic target forms: enemy carriers, dropped enemy intel, and friendly carrier
escorts. The alpha dropped-enemy branch no longer requires the legacy proof
graph before it can call `TryRouteToDirectSeekTarget`; legacy behavior retains
that proof gate. Focused coverage passes `4/4`: the three alpha dynamic-target
tests plus the existing legacy Engineer enemy-carrier regression.

## 2026-07-29 v29 live revalidation checkpoint

The first v29 Truefort screen was incorrectly run with only `1,500` simulated
capture ticks. That budget is shorter than the valid class/team return time;
it produced `0/20` timeouts even though a longer Scout trial captured at tick
`2,130`. This was a test-budget error, not evidence of a broken graph.

With the v29 graph cached and a `3,000`-tick accelerated budget, Truefort is
validated again at `20/20`. The clean v29 rebuild produced `669` nodes and
`42,652` edges in `260.5` seconds; the full cache-only matrix completed in
`171.6` seconds, with the slowest capture at tick `2,944`.

The current v29 live-map evidence beyond Truefort is not yet a full pass:

- `cp_gully`: `19/20`; only Blue Pyro failed in the shared full-roster graph.
  The same Pyro passes against a Pyro-only diagnostic graph, so this is not
  accepted as class-agnostic graph coverage and remains a shared-graph/runtime
  issue to isolate.
- `cp_thundermountain_d`: `0/20`; nearest assigned capture-zone selection
  corrected the target coordinates, but point-state progression still fails.

All current capture acceptance screens use at least `3,000` simulated ticks;
`1,500` is no longer a valid acceptance budget for the roster. Graph builds
remain serialized per map, and capture trials reuse the resulting cache.

The v29 regression screen has now added two more current full passes:

- `Eiger`: graph `392` nodes / `28,308` edges, cold build `88.4` seconds,
  cache-only matrix `20/20` in `45.7` seconds; slowest capture tick `1,915`.
- `Corinth`: graph `334` nodes / `22,905` edges, cold build `81.3` seconds;
  the four-at-a-time cache-only matrix completed `20/20` in approximately
  three minutes, with all KOTH captures completing by tick `1,052`.

This makes the current v29 accepted set `3/43` unique representatives, with
`40` still requiring a current full roster pass. The 16 no-graph packages and
the duplicate walkmask aliases remain excluded from that denominator.

`cp_docking_v2` is now also v29-validated at `20/20` (all classes and both
teams). Its shared graph has `290` nodes and `18,528` edges, built cold in
`86.1` seconds; the cache-only matrix completed in about `43` seconds. The
friendly `vip_docking` package remains excluded as a no-graph alias.

The current accepted set is therefore `4/43`, with `39` representatives left.

`Harvest` is now v29-validated at `20/20`. Its shared graph has `221` nodes
and `12,871` edges, built cold in `54.7` seconds; the four-at-a-time matrix
completed in about `63` seconds, with all captures completing by tick `1,052`.
The current accepted set is `5/43`, leaving `38` representatives.

`Conflict` is now v29-validated at `20/20` on its `380`-node / `22,912`-
edge shared graph. The cold build took `95.0` seconds; the five-batch
cache-only matrix completed in about `58` seconds, with the slowest capture
at tick `1,435`. The current accepted set is `6/43`, leaving `37`.

`Gallery` is now v29-validated at `20/20`. Its graph has `360` nodes and
`23,240` edges, built cold in `79.9` seconds; the five-batch cache-only
matrix completed in about `175` seconds, with all captures completing by
tick `1,052`. The current accepted set is `7/43`, leaving `36`.

`Valley` is now v29-validated at `20/20`. Its graph has `438` nodes and
`26,928` edges, built cold in `139.8` seconds; the five-batch cache-only
matrix completed in about `86` seconds, with all captures completing by tick
`1,052`. The historical eight-map set is therefore fully revalidated under
v29, and the current accepted set is `8/43`, leaving `35` representatives.

The follow-up `3cp_kistra` cache-only screen now passes `20/20` after the
alpha capture-staging bypass. The v29 graph remains `482` nodes / `29,734`
edges, and all ten classes pass for both teams. This raises the accepted set
to `9/43`, leaving `34` representatives; the earlier `19/20` screen is
retained only as the pre-fix diagnostic result.

The focused `cp_gully` Blue Pyro rerun also passed on the shared full-roster
graph at tick `1,519` after one recoverable missed-jump edge. A complete
four-way cache-only matrix then passed `20/20` at the `3,000`-tick budget.
The earlier `19/20` result was a `1,500`-tick screen and is superseded. The
accepted set is now `10/43`, leaving `33` representatives.

The earlier `cp_thundermountain_d` `0/20` result is withdrawn from acceptance
accounting. Its saved screen used per-class graph caches rather than the
shared full-roster cache, and the map is Attack/Defense setup mode: all points
initialize Blue, so the current generic “changed to my team” predicate can
never pass a Blue trial. This requires a mode-aware acceptance contract
(attacker capture progression versus defender traversal/hold), not generator
tuning, before it can be screened fairly.

`cp_coldfront_v7` is now v29-validated at `20/20`. Its shared graph has `420`
nodes and `23,399` edges; the cold build took `289.7` seconds, while the
five-batch cache-only matrix completed in about `68` seconds. The current
accepted set is `11/43`, leaving `32` representatives.

`ctf_conflict2_v3` has a valid v29 structural artifact (`346` nodes,
`21,602` edges, cold build `151.0` seconds), but its live acceptance is
quarantined. Red Engineer/Soldier trials reach enemy intel and then enter a
long-tail return edge that exceeds the bounded runner; the partial fanout was
stopped and is not counted. This is a targeted carrier-return investigation,
not evidence to weaken the graph gate or to accept a partial roster.

`koth_cdragon` has a v29 structural artifact (`273` nodes, `18,565` edges,
cold build `51.6` seconds), but its live screen is quarantined. The capture
zone-adjacent surface is a disconnected moving-platform component; the
reachable route stops on a lower surface at y≈1074 while the KOTH zone is at
y≈891. This is a dynamic-surface connectivity gap to solve explicitly, not a
class-by-class acceptance failure.

`koth_crab_v2` is now v29-validated at `20/20` after an alpha-only capture
arrival hold was added in the controller. The graph itself was unchanged:
`309` nodes and `22,222` edges, loaded from the existing cache. The original
screen was `17/20`; the three failing Blue cases (Pyro, Soldier, and Sniper)
all reached the final objective node but carried residual horizontal momentum
past the capture zone. The focused rerun passed all three against the shared
ten-class graph, and the complete five-batch cache-only matrix passed all ten
classes for both teams. Captures completed at ticks `976`â€“`1,052`. This raises
the accepted v29 set to `12/43`, leaving `31` representatives.

The arrival hold is runtime steering behavior, not a generator exception: it
only engages for alpha capture/KOTH goals after graph-route completion, brakes
or centers the bot inside the active zone, and leaves legacy navigation
unchanged. Because the graph fingerprint did not change, no graph rebuild was
needed for this validation.

`ctf_conflict_plx` is now v29-validated at `20/20`. Its current shared graph
has `379` nodes and `22,805` edges (cache hit after the current full-roster
key was built once). The five-batch cache-only matrix passed all ten classes
for both teams, with capture ticks from `868` to `1,435`. The accepted set is
now `13/43`, leaving `30` representatives.

`ctf_conflict_pro` is now v29-validated at `20/20`. Its shared graph has
`382` nodes and `22,847` edges, built once in `87.2` seconds; the subsequent
five-batch cache-only matrix passed all ten classes for both teams, with
capture ticks from `854` to `1,455`. The accepted set is now `14/43`, leaving
`29` representatives.

`koth_eureka_r2` is currently quarantined after its v29 cache-only matrix
completed. The graph built in `38.8` seconds with `283` nodes and `15,301`
edges, but most class/team trials stopped at different final surface nodes
around xâ‰ˆ1900 or xâ‰ˆ2570 while the KOTH objective was at xâ‰ˆ2244. Only one
trial reached capture. This is a shared graph-to-objective connectivity issue,
not a reason to add per-class exceptions, so it is not counted as a pass.

`koth_stonemine` is also quarantined for investigation: its cold v29 report
produced no graph result within the five-minute diagnostic ceiling and was
stopped after verifying the exact orphaned report process. No cache or live
acceptance result was created. This is now tracked as a generator performance
case rather than inferred from the map package's small file size.

`ctf_conflict_srs_red` is now v29-validated at `20/20`. Its shared graph has
`385` nodes and `23,717` edges, built once in `129.0` seconds; the five-batch
cache-only matrix passed all ten classes for both teams, with capture ticks
from `727` to `1,413`. The accepted set is now `15/43`, leaving `28`
representatives.

`koth_AMERICA` is quarantined as another generator-performance case. Its
v29 graph report remained CPU-bound past the three-minute diagnostic ceiling
without emitting a graph result; the exact report process was stopped. No
cache or live acceptance result exists, so it is not counted as a pass.

`koth_trestle` is now v29-validated at `20/20`. Its shared graph has `281`
nodes and `15,431` edges, built once in `49.0` seconds; the five-batch
cache-only matrix passed all ten classes for both teams, with captures from
tick `976` to `1,052`. The accepted set is now `17/43`, leaving `26`
representatives.

`ctf_RSM` is quarantined after the accelerated screen exposed a carrier-return
failure: Red Scout reached enemy intel but timed out at tick `3,000` still
carrying it, while the opposite Scout completed. The remaining full fanout was
stopped after this disqualifying shared-route result to avoid spending several
minutes on a matrix that cannot be accepted. It is not counted as a pass.

`koth_standoff` is now v29-validated at `20/20`. Its shared graph has `291`
nodes and `18,563` edges, built once in `67.9` seconds; the five-batch
cache-only matrix passed all ten classes for both teams, with captures from
tick `976` to `1,052`. The accepted set is now `17/43`, leaving `26`
representatives.

## 2026-07-29 current sweep reconciliation

The current v29 accepted representatives are `17/43`:
Truefort, Eiger, Corinth, `cp_docking_v2`, Harvest, Conflict, Gallery,
Valley, `3cp_kistra`, `cp_gully`, `cp_coldfront_v7`, `koth_crab_v2`,
`ctf_conflict_plx`, `ctf_conflict_pro`, `ctf_conflict_srs_red`,
`koth_trestle`, and `koth_standoff`. Every accepted representative has a
full `20/20` matrix: all ten classes on both teams, with a shared ten-class
graph and a `3,000`-tick accelerated budget.

The current non-accepted cases are intentionally separated by failure type:
`koth_eureka_r2` has a shared graph-to-objective connectivity gap;
`ctf_RSM` has a Red carrier-return timeout; `ctf_conflict2_v3` has a
long-tail carrier-return issue; `koth_cdragon` has a moving-platform
connectivity gap; `cp_thundermountain_d` needs an Attack/Defense-specific
acceptance contract; and `koth_stonemine`/`koth_AMERICA` exceeded the cold
graph diagnostic ceiling without producing a result. None of these are
counted as partial passes.

For iteration speed, a generator-affecting change requires one serialized cold
build per unique walkmask representative. Controller/steering changes reuse
the persistent graph cache. Runtime acceptance trials are launched in five
batches of four independent class/team processes; this keeps a full matrix
near one to three minutes on cache hits and avoids duplicate cold builds.
