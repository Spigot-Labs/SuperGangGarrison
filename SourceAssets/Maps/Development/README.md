# Development map packages

These map packages are retained as manual-development and builder fixtures. They are not part of the shipped map set; release packaging consumes packages from the repository-level `Maps/` directory.

Promote a map to `Maps/` only when it is intended to ship and has been added to the appropriate catalog or rotation configuration.
